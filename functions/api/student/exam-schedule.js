import {q} from "../_shared/db.js";import {authenticate} from "../_shared/auth.js";import {ok,errors} from "../_shared/response.js";import {withErrorHandling} from "../_shared/validate.js";
export const onRequestGet=withErrorHandling(async({request,env})=>{const {user}=await authenticate(request,env);const db=q(env);const r=await db.all(`SELECT es.*,e.title,c.name class_name,s.name subject_name FROM exam_schedules es JOIN exams e ON e.id=es.exam_id JOIN classes c ON c.id=e.class_id JOIN subjects s ON s.id=e.subject_id JOIN class_students cs ON cs.class_id=e.class_id JOIN students st ON st.id=cs.student_id WHERE es.school_id=? AND st.user_id=? ORDER BY es.scheduled_date,es.start_time`,user.school_id,user.id);return ok(r.results);});
