import {q} from "../_shared/db.js";import {authenticate} from "../_shared/auth.js";import {ok} from "../_shared/response.js";import {withErrorHandling} from "../_shared/validate.js";
export const onRequestGet=withErrorHandling(async({request,env})=>{const {user}=await authenticate(request,env);const db=q(env);const books=await db.all(`SELECT id,title,author,isbn,category,available_copies,total_copies FROM library_books WHERE school_id=? ORDER BY title`,user.school_id);const loans=await db.all(`SELECT l.*,b.title FROM library_loans l JOIN library_books b ON b.id=l.book_id JOIN students s ON s.id=l.student_id WHERE l.school_id=? AND s.user_id=? ORDER BY l.borrowed_at DESC`,user.school_id,user.id);return ok({books:books.results,loans:loans.results});});
