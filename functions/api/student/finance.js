import {q} from "../_shared/db.js";import {authenticate} from "../_shared/auth.js";import {ok} from "../_shared/response.js";import {withErrorHandling} from "../_shared/validate.js";
export const onRequestGet=withErrorHandling(async({request,env})=>{const {user}=await authenticate(request,env);const db=q(env);const r=await db.all(`SELECT sf.*,fi.name fee_name,COALESCE((SELECT SUM(amount) FROM payments WHERE student_fee_id=sf.id),0) paid FROM student_fees sf JOIN fee_items fi ON fi.id=sf.fee_item_id JOIN students st ON st.id=sf.student_id WHERE st.user_id=? AND sf.school_id=? ORDER BY sf.id DESC`,user.id,user.school_id);return ok(r.results);});
