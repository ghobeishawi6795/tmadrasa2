import {q} from "../_shared/db.js";import {authenticate,requirePermission} from "../_shared/auth.js";import {ok} from "../_shared/response.js";import {withErrorHandling} from "../_shared/validate.js";
export const onRequestGet=withErrorHandling(async({request,env})=>{const {user}=await authenticate(request,env);await requirePermission(env,user,'exams.view');const db=q(env);const r=await db.all(`SELECT e.id,e.title,e.start_at,e.end_at,e.status,c.name class_name,s.name subject_name FROM exams e JOIN classes c ON c.id=e.class_id JOIN subjects s ON s.id=e.subject_id WHERE e.school_id=? AND e.deleted_at IS NULL ORDER BY e.start_at DESC LIMIT 300`,user.school_id);return ok(r.results);});
