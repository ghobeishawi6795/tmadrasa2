import {q} from "../_shared/db.js";import {authenticate} from "../_shared/auth.js";import {getParentRecord} from "../_shared/ownership.js";import {ok} from "../_shared/response.js";import {withErrorHandling} from "../_shared/validate.js";
export const onRequestGet=withErrorHandling(async({request,env})=>{const {user}=await authenticate(request,env);const p=await getParentRecord(env,user.id),db=q(env);const r=await db.all(`SELECT DISTINCT t.id,u.full_name FROM teachers t JOIN users u ON u.id=t.user_id JOIN class_teachers ct ON ct.teacher_id=t.id JOIN class_students cs ON cs.class_id=ct.class_id JOIN parent_students ps ON ps.student_id=cs.student_id WHERE t.school_id=? AND ps.parent_id=? AND t.deleted_at IS NULL ORDER BY u.full_name`,user.school_id,p.id);return ok(r.results);});
