import {q} from "../_shared/db.js";import {authenticate} from "../_shared/auth.js";import {getParentRecord} from "../_shared/ownership.js";import {ok} from "../_shared/response.js";import {withErrorHandling} from "../_shared/validate.js";
export const onRequestGet=withErrorHandling(async({request,env})=>{const {user}=await authenticate(request,env);const p=await getParentRecord(env,user.id),db=q(env);const r=await db.all(`SELECT DISTINCT es.*,e.title,c.name class_name,s.name subject_name,su.full_name student_name FROM exam_schedules es JOIN exams e ON e.id=es.exam_id JOIN classes c ON c.id=e.class_id JOIN subjects s ON s.id=e.subject_id JOIN class_students cs ON cs.class_id=e.class_id JOIN students st ON st.id=cs.student_id JOIN users su ON su.id=st.user_id JOIN parent_students ps ON ps.student_id=st.id WHERE es.school_id=? AND ps.parent_id=? ORDER BY es.scheduled_date,es.start_time`,user.school_id,p.id);return ok(r.results);});
