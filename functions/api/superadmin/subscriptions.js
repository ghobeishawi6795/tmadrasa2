import {q} from "../_shared/db.js";import {authenticate,requireRole} from "../_shared/auth.js";import {ok,errors} from "../_shared/response.js";import {readJson,requireFields,withErrorHandling} from "../_shared/validate.js";
export const onRequestGet=withErrorHandling(async({request,env})=>{const {roles}=await authenticate(request,env);requireRole(roles,'super_admin');const db=q(env);const r=await db.all(`SELECT id,name,plan,subscription_status,subscription_expires_at,trial_ends_at,active FROM schools WHERE id<>0 ORDER BY id DESC`);return ok(r.results);});
export const onRequestPatch=withErrorHandling(async({request,env})=>{const {user,roles}=await authenticate(request,env);requireRole(roles,'super_admin');const b=await readJson(request);requireFields(b,['school_id']);const db=q(env);if(b.status && !['active','past_due','cancelled','suspended'].includes(b.status))throw errors.validation('وضعیت اشتراک نامعتبر است');await db.run(`UPDATE schools SET plan=COALESCE(?,plan),subscription_status=COALESCE(?,subscription_status),subscription_expires_at=COALESCE(?,subscription_expires_at),trial_ends_at=COALESCE(?,trial_ends_at),active=CASE WHEN ?='suspended' THEN 0 ELSE active END WHERE id=?`,b.plan||null,b.status||null,b.expires_at||null,b.trial_ends_at||null,b.status||'',b.school_id);return ok(null,'اشتراک بروزرسانی شد');});
