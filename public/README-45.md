# v28-merged (2026-09-14) — ادغام ۴۱ باگ اصلاح‌شده‌ی شما با فیچر «ضعیف‌ترین فصل‌ها»

این نسخه روی همون کد پایه‌ای ساخته شده که شما با ۴۱ اصلاح امنیتی/منطقی خودتون آپلود کردید (شامل مایگریشن جدید `028_security_integrity.sql`) — فیچر «ضعیف‌ترین فصل‌ها» (که قبلاً روی v27 ساخته بودم) روی همین نسخه‌ی اصلاح‌شده پیاده شد، نه روی نسخه‌ی قدیمی.

### ⚠️ قدم دستی لازم قبل از دیپلوی
مایگریشن `028_security_integrity.sql` (مال اصلاحات خودتون) رو اگه هنوز روی D1 اجرا نکردید، حتماً قبل از دیپلوی این نسخه اجرا کنید:
```bash
wrangler d1 execute madrese --remote --file=database/migrations/028_security_integrity.sql
```
فیچر ضعیف‌ترین فصل‌ها هیچ مایگریشن جدیدی لازم نداره (از جدول‌های موجود `student_practice_results` و `questions` استفاده می‌کنه).

### روش ادغام (برای شفافیت)
فایل‌هایی که هر دو طرف (اصلاحات شما + فیچر من) بهشون دست زده بودن رو چک کردم:
- `_shared/ownership.js`, `teacher/grades.js`, `student/practice-questions.js`: فقط شما تغییر داده بودید (بخش‌های مربوط به fix)، فیچر من فایل جدا (`practice-weak-chapters.js`) اضافه کرد — بدون تداخل.
- `_shared/practice.js`, `student/index.html`, `teacher/index.html`: هیچ‌کدوم رو اصلاحات شما لمس نکرده بود — نسخه‌ی من مستقیم جایگزین شد.
- `parent/index.html`: شما یک خط رو (XSS در نمایش برنامه‌ی هفتگی، بخش schedule) اصلاح کرده بودید؛ فیچر من توی تابع جدای `renderGrades()` اضافه شد — هر دو تغییر با هم merge شدن، چیزی از اصلاح شما حذف نشد.

### فیچر «ضعیف‌ترین فصل‌ها» (خلاصه)
- تابع مشترک `getWeakestChapters` روی `student_practice_results` (فقط نتایج تصحیح‌خودکار) گروه‌بندی می‌کنه، با حداقل ۳ سؤال جواب‌داده‌شده در هر فصل تا نویز حساب نشه.
- **دانش‌آموز**: بالای صفحه‌ی تمرین.
- **والد**: توی صفحه‌ی «وضعیت تحصیلی».
- **معلم**: پایین صفحه‌ی «نمرات»، دکمه‌ی «مشاهده» برای هر دانش‌آموزِ کلاس/درسِ انتخاب‌شده، با همون چک مالکیتی ثبت نمره.

---
با این نسخه، هم ۴۱ باگ شما و هم ۴ موردِ فالوآپِ فیچر تمرین (خلاصه، تمرین سریع، تب فصل، ضعیف‌ترین فصل‌ها) توی یک کد یکپارچه‌ان.
